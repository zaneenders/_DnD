/*
TODO: Generate `.swift` file containing the text use to display as character creation
*/
enum CharacterGenerator {}

func armor() -> String {
    // Not finished
    let roll = Range(1...20)
    switch roll {
        case Range(1...3):
        ()
        case Range(4...14):
        ()
        default:
        ()
    }
    return ""
}